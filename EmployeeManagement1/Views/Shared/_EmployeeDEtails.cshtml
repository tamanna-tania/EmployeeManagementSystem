@*@model Employee 
@{
    var photoPath = "~/images/" + (Model.PhotoPath ?? "images (1).jpg");
 }

@if (Model==null)
{
    <div class="card-header">
        <h3>No Data to show!</h3>
    </div>
}
else
{
<div class="card m-3" style="min-width:18rem; max-width:30%">
    <div class="card-header">
        <h3>@Model.Name</h3>
    </div>

    <img class="card-img-top" src="~/images/car.jpg" />
    <img class="card-img-top imageThumbail" src="@photoPath" asp-append-version="true" />
    <div>
        <h4>Employee ID: @Model.Id</h4>
        <h4>Email: @Model.Email</h4>
        <h4>Department: @Model.Department</h4>

        <p>@Model.Email</p>
        <p>@Model.Department</p>
    </div>


    <div class="card-footer text-center">
        <form asp-controller="Home" asp-action="Delete" asp-route-id="@Model.Id" method="post">
            <a asp-controller="Home" asp-action="Details" asp-route-id="@Model.Id" class="btn btn-primary">View</a>
            <a asp-controller="Home" asp-action="Update" asp-route-id="@Model.Id" class="btn btn-info">Edit</a>

            <span id="deleteSpan_@Model.Id">
                <a href="#" class="btn btn-danger" onclick="confirmDelete('@Model.Id',true)">Delete</a>
            </span>
            <span id="confirmDeleteSpan_@Model.Id" style="display:none">
                <span>
                    <br />Are you sure to delete this record?
                </span>
                <button type="submit" class="btn btn-danger">Yes</button>
                <a href="#" class="btn btn-primary" onclick="confirmDelete('@Model.Id',false)">No</a>
            </span>

        </form>


    </div>
</div>
}*@
